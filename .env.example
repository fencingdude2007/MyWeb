# Copy to .env for local (non-Docker) development.
# docker-compose.yml sets these itself for containers.
DATABASE_URL=postgresql+asyncpg://myweb:myweb@localhost:5432/myweb

# Coming in later phases:
# ANTHROPIC_API_KEY=            # Phase 2 (summarization)
# GOOGLE_CLIENT_ID=             # Phase 1 (OAuth)
# GOOGLE_CLIENT_SECRET=         # Phase 1 (OAuth)
